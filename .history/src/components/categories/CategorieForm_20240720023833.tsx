import { useNavigate, useParams } from "react-router-dom"

export default function CategorieForm() {
    const { id } = useParams();
    const useNavige = useNavigate();

    const [categorie,setCategorie]=use
    return (
        <>
        </>
    )
}