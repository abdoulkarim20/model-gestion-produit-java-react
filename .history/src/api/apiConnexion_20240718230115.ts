import { CategorieDTO } from "../models/categorie/categorieDTO";

const apiConnexion={
    getAllCategories:async ():Promise<CategorieDTO[]>=>{
        try {
            const response=
            
        } catch (error) {
            
        }
    }
}